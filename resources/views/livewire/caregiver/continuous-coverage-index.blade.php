<div class="hc-page space-y-5 py-5 sm:space-y-6 sm:py-8">
    @if (session('status'))
        <x-alert color="green">{{ session('status') }}</x-alert>
    @endif

    @foreach($upcoming as $shift)<span id="coverage-shift-{{ $shift->id }}" class="block scroll-mt-24" aria-hidden="true"></span>@endforeach

    <section class="hc-brand-panel">
        <div class="flex flex-col gap-5 lg:flex-row lg:items-end lg:justify-between">
            <div><p class="hc-brand-kicker text-[#E8E0FF]">Continuous Coverage</p><h1 class="mt-1 font-display text-3xl font-semibold text-white sm:text-4xl">Your coverage commitments</h1><p class="mt-2 max-w-2xl text-base text-[#F7F1E8]/82">You choose which family-approved teams, recurring lanes, and optional backup shifts fit your availability.</p></div>
            <div class="grid grid-cols-2 gap-3"><div class="rounded-2xl border border-white/20 bg-white/10 p-3"><p class="text-xs text-white/70">Upcoming</p><p class="mt-1 text-2xl font-semibold">{{ $upcoming->count() }}</p></div><div class="rounded-2xl border border-white/20 bg-white/10 p-3"><p class="text-xs text-white/70">Offers</p><p class="mt-1 text-2xl font-semibold">{{ $invitations->count()+$lanes->where('status','offered')->count()+$offers->count() }}</p></div></div>
        </div>
    </section>

    <div class="rounded-2xl border border-[#D8D0C5] bg-[#FFF9F1] p-4 text-sm text-[#4B5B6B]"><p class="font-semibold text-[#17313F]">You stay in control of your schedule.</p><p class="mt-1">A family’s approval lets them invite you. Nothing becomes your commitment until you accept it.</p></div>

    <nav class="overflow-x-auto rounded-2xl border border-[#DED6CA] bg-white p-1" aria-label="Continuous Coverage sections"><div class="flex min-w-max gap-1">@foreach(['schedule'=>'Schedule','offers'=>'Offers','commitments'=>'Commitments','history'=>'History & earnings'] as $key=>$label)<button wire:click="setTab('{{ $key }}')" class="min-h-11 rounded-xl px-4 text-sm font-semibold {{ $tab===$key?'bg-[#0F3D3E] text-white':'text-[#526474] hover:bg-[#F7F2EA]' }}" @if($tab===$key) aria-current="page" @endif>{{ $label }}@if($key==='offers' && ($invitations->count()+$lanes->where('status','offered')->count()+$offers->count())>0) <span class="ml-1 rounded-full bg-white/20 px-2 py-0.5">{{ $invitations->count()+$lanes->where('status','offered')->count()+$offers->count() }}</span>@endif</button>@endforeach</div></nav>

    @if($tab === 'schedule')
        <section class="space-y-4"><div><h2 class="font-display text-2xl font-semibold">Upcoming coverage shifts</h2><p class="text-sm text-[#607080]">Confirmed shifts use the normal LoLo Care visit flow for check-in, time, and earnings.</p></div><div class="space-y-4">@forelse($upcoming->groupBy(fn($shift)=>$shift->scheduled_start_at->copy()->setTimezone($shift->plan->timezone)->toDateString()) as $date=>$shifts)<section><h3 class="mb-2 text-sm font-semibold uppercase tracking-wide text-[#2F6F62]">{{ \Illuminate\Support\Carbon::parse($date)->format('l, F j') }}</h3><div class="space-y-3">@foreach($shifts as $shift)<article class="rounded-3xl border border-[#DED6CA] bg-white p-5"><div class="flex flex-col gap-4 md:flex-row md:items-start md:justify-between"><div><div class="flex flex-wrap items-center gap-2"><p class="font-display text-xl font-semibold">{{ $shift->scheduled_start_at->copy()->setTimezone($shift->plan->timezone)->format('g:i A') }} – {{ $shift->scheduled_end_at->copy()->setTimezone($shift->plan->timezone)->format('g:i A') }}</p><span class="rounded-full px-3 py-1 text-xs font-semibold {{ $shift->status==='payment_attention'?'bg-amber-100 text-amber-800':($shift->status==='in_progress'?'bg-sky-100 text-sky-800':'bg-emerald-100 text-emerald-800') }}">{{ ucfirst(str_replace('_',' ',$shift->status)) }}</span></div><p class="mt-1 font-semibold text-[#324457]">{{ $shift->plan->title }}</p><p class="mt-1 text-sm text-[#607080]">Family: {{ $shift->plan->family?->name }} · {{ number_format($shift->scheduled_minutes/60,1) }} hours</p></div><div class="flex flex-col gap-2 sm:flex-row"><a href="{{ route('caregiver.shifts.index') }}" wire:navigate class="inline-flex min-h-11 items-center justify-center rounded-xl bg-[#0F3D3E] px-4 font-semibold text-white">Open visit</a></div></div><details class="mt-4 rounded-2xl bg-[#F7F2EA]"><summary class="min-h-11 cursor-pointer list-none px-4 py-3 font-semibold text-[#324457]">Handoff and release options</summary><div class="space-y-4 border-t border-[#E4DDD3] p-4">@if($shift->handoffs->isNotEmpty())<section aria-label="Existing handoff notes"><p class="text-sm font-semibold">Handoff notes for this shift</p><div class="mt-2 space-y-2">@foreach($shift->handoffs as $handoff)<article class="rounded-xl border border-[#DED6CA] bg-white p-3"><p class="text-sm text-[#324457]">{{ $handoff->notes }}</p><p class="mt-1 text-xs text-[#7B8794]">{{ $handoff->caregiver?->name ?: 'Caregiver' }} · {{ $handoff->recorded_at->setTimezone($shift->plan->timezone)->format('M j, g:i A') }}</p></article>@endforeach</div></section>@endif<div><label class="text-sm font-semibold">Add a handoff note</label><textarea wire:model="handoffNotes.{{ $shift->id }}" rows="3" class="mt-1 w-full rounded-xl border-[#CFC4B5]" placeholder="Useful context for the next caregiver or family."></textarea>@error('handoffNotes.'.$shift->id)<p class="text-sm text-rose-700">{{ $message }}</p>@enderror<button wire:click="saveHandoff({{ $shift->id }})" wire:loading.attr="disabled" class="mt-2 min-h-11 rounded-xl border border-[#2F6F62] px-4 font-semibold text-[#2F6F62] disabled:opacity-60">Save handoff note</button></div>@if($shift->scheduled_start_at->isFuture())<div class="border-t border-[#E4DDD3] pt-4"><label class="text-sm font-semibold text-rose-800">Need to release this shift?</label><textarea wire:model="releaseReasons.{{ $shift->id }}" rows="2" class="mt-1 w-full rounded-xl border-rose-200" placeholder="Tell the family why a replacement is needed."></textarea>@error('releaseReasons.'.$shift->id)<p class="text-sm text-rose-700">{{ $message }}</p>@enderror<button wire:click="releaseShift({{ $shift->id }})" wire:confirm="Release this confirmed shift and invite eligible family-approved backups?" wire:loading.attr="disabled" class="mt-2 min-h-11 rounded-xl bg-rose-700 px-4 font-semibold text-white disabled:opacity-60">Release shift</button></div>@endif</div></details></article>@endforeach</div></section>@empty<div class="rounded-3xl border border-dashed border-[#CFC4B5] bg-white p-8 text-center"><h2 class="font-display text-xl font-semibold">No upcoming coverage shifts</h2><p class="mt-2 text-[#607080]">Accepted recurring lanes and replacement shifts will appear here.</p></div>@endforelse</div></section>
    @elseif($tab === 'offers')
        <section class="space-y-6">
            @foreach($invitations as $member)<span id="team-invitation-{{ $member->id }}" class="block scroll-mt-24" aria-hidden="true"></span>@endforeach
            @foreach($lanes->where('status','offered') as $lane)<span id="lane-offer-{{ $lane->id }}" class="block scroll-mt-24" aria-hidden="true"></span>@endforeach
            @foreach($offers as $offer)<span id="replacement-offer-{{ $offer->id }}" class="block scroll-mt-24" aria-hidden="true"></span>@endforeach
            @if($applications->isNotEmpty())
                <section class="rounded-3xl border border-amber-200 bg-amber-50 p-5" aria-labelledby="pending-coverage-applications">
                    <h2 id="pending-coverage-applications" class="font-display text-2xl font-semibold">Applications waiting for family review</h2>
                    <p class="mt-1 text-sm text-[#526474]">The family must approve you, and you must then choose whether to join, before any coverage can be offered.</p>
                    <div class="mt-4 grid gap-3 lg:grid-cols-2">@foreach($applications as $application)<article class="rounded-2xl border border-amber-200 bg-white p-4"><p class="font-semibold">{{ $application->plan->coverage_pattern === '24_7' ? '24/7 Continuous Coverage' : ucfirst($application->plan->coverage_pattern).' coverage' }}</p><p class="mt-1 text-sm text-[#607080]">Applied {{ $application->created_at->diffForHumans() }} · begins {{ $application->plan->starts_on->format('M j, Y') }}</p><span class="mt-3 inline-flex rounded-full bg-amber-100 px-3 py-1 text-xs font-semibold text-amber-900">Waiting for family review</span></article>@endforeach</div>
                </section>
            @endif
            @if($opportunities->isNotEmpty())
                <div>
                    <div><h2 class="font-display text-2xl font-semibold">Coverage plans accepting applications</h2><p class="mt-1 text-sm text-[#607080]">Applying asks the family to review your profile. It does not add you to the care team or assign a shift.</p></div>
                    <div class="mt-3 grid gap-4 lg:grid-cols-2">
                        @foreach($opportunities as $opportunity)
                            @php $opportunityArea=collect([data_get($opportunity->address_snapshot,'city'),data_get($opportunity->address_snapshot,'state'),data_get($opportunity->address_snapshot,'zip')])->filter()->implode(', '); $opportunityTasks=collect((array)$opportunity->task_snapshot)->pluck('name')->filter()->take(4)->implode(', '); @endphp
                            <article class="rounded-3xl border border-[#DED6CA] bg-white p-5" wire:key="coverage-opportunity-{{ $opportunity->id }}">
                                <p class="text-xs font-semibold uppercase tracking-wide text-[#2F6F62]">Application opportunity</p>
                                <h3 class="mt-2 font-display text-xl font-semibold">{{ $opportunity->coverage_pattern === '24_7' ? '24/7 Continuous Coverage' : ucfirst($opportunity->coverage_pattern).' coverage' }}</h3>
                                <dl class="mt-3 space-y-1 text-sm text-[#526474]"><div><dt class="inline font-semibold">Service area:</dt> <dd class="inline">{{ $opportunityArea ?: 'Area available after family approval' }}</dd></div><div><dt class="inline font-semibold">Begins:</dt> <dd class="inline">{{ $opportunity->starts_on->format('M j, Y') }} · {{ $opportunity->timezone }}</dd></div><div><dt class="inline font-semibold">Shift structure:</dt> <dd class="inline">{{ $opportunity->coverage_pattern === '24_7' ? number_format($opportunity->shift_length_minutes/60, 1).'-hour rotating shifts' : ($opportunity->coverage_pattern === 'overnight' ? 'Nightly overnight window' : 'Custom weekly windows') }}</dd></div><div><dt class="inline font-semibold">Care activities:</dt> <dd class="inline">{{ $opportunityTasks ?: 'General coverage expectations' }}</dd></div><div><dt class="inline font-semibold">Estimated caregiver earnings:</dt> <dd class="inline">{{ $planEarningEstimates->get($opportunity->id) }}</dd></div></dl>
                                <p class="mt-3 rounded-xl bg-[#F7F2EA] p-3 text-sm text-[#526474]">Recipient identity, exact address, and private care notes remain hidden unless the family approves you and you choose to join.</p>
                                <button wire:click="applyToPlan({{ $opportunity->id }})" wire:confirm="Send your caregiver profile to this family for review? This does not assign you to any coverage." wire:loading.attr="disabled" class="mt-4 min-h-12 w-full rounded-xl bg-[#0F3D3E] px-4 font-semibold text-white disabled:opacity-60">Apply for family review</button>
                            </article>
                        @endforeach
                    </div>
                </div>
            @endif
            @if($invitations->isNotEmpty())<div><h2 class="font-display text-2xl font-semibold">Care-team invitations</h2><div class="mt-3 grid gap-4 lg:grid-cols-2">@foreach($invitations as $member)@php $invitationArea=collect([data_get($member->plan->address_snapshot,'city'),data_get($member->plan->address_snapshot,'state')])->filter()->implode(', '); $invitationTasks=collect((array)$member->plan->task_snapshot)->pluck('name')->filter()->take(4)->implode(', '); @endphp<article class="rounded-3xl border border-violet-200 bg-violet-50 p-5"><p class="text-xs font-semibold uppercase tracking-wide text-violet-700">Family approved you</p><h3 class="mt-2 font-display text-xl font-semibold">Care-team invitation from {{ $member->plan->family?->name }}</h3><dl class="mt-3 space-y-1 text-sm text-[#526474]"><div><dt class="inline font-semibold">Service area:</dt> <dd class="inline">{{ $invitationArea ?: 'Available after joining' }}</dd></div><div><dt class="inline font-semibold">Coverage:</dt> <dd class="inline">{{ $member->plan->coverage_pattern === '24_7' ? '24/7 in '.number_format($member->plan->shift_length_minutes/60, 1).'-hour shifts' : ucfirst($member->plan->coverage_pattern).' recurring coverage' }}</dd></div><div><dt class="inline font-semibold">Begins:</dt> <dd class="inline">{{ $member->plan->starts_on->format('M j, Y') }} · {{ $member->plan->timezone }}</dd></div><div><dt class="inline font-semibold">Care activities:</dt> <dd class="inline">{{ $invitationTasks ?: 'General coverage expectations' }}</dd></div><div><dt class="inline font-semibold">Estimated caregiver earnings:</dt> <dd class="inline">{{ $planEarningEstimates->get($member->plan->id) }}</dd></div></dl><p class="mt-3 text-sm text-[#526474]">Joining lets you review recurring or backup opportunities. It does not assign you to any shift. Recipient details stay private until you join.</p><div class="mt-4 grid grid-cols-2 gap-2"><button wire:click="acceptTeam({{ $member->id }})" wire:loading.attr="disabled" class="min-h-12 rounded-xl bg-violet-800 px-3 font-semibold text-white disabled:opacity-60">Join care team</button><button wire:click="declineTeam({{ $member->id }})" wire:loading.attr="disabled" class="min-h-12 rounded-xl border border-violet-300 px-3 font-semibold text-violet-800 disabled:opacity-60">Decline</button></div></article>@endforeach</div></div>@endif
            @if($lanes->where('status','offered')->isNotEmpty())<div><h2 class="font-display text-2xl font-semibold">Recurring coverage offers</h2><div class="mt-3 grid gap-4 lg:grid-cols-2">@foreach($lanes->where('status','offered') as $lane)@php $laneArea=collect([data_get($lane->plan->address_snapshot,'city'),data_get($lane->plan->address_snapshot,'state'),data_get($lane->plan->address_snapshot,'zip')])->filter()->implode(', '); $laneTasks=collect((array)$lane->plan->task_snapshot)->pluck('name')->filter()->take(4)->implode(', '); @endphp<article class="rounded-3xl border border-amber-200 bg-amber-50 p-5"><p class="text-xs font-semibold uppercase tracking-wide text-amber-800">Your decision</p><h3 class="mt-2 font-display text-xl font-semibold">{{ ['Sunday','Monday','Tuesday','Wednesday','Thursday','Friday','Saturday'][$lane->day_of_week] }} · {{ \Illuminate\Support\Carbon::parse($lane->starts_at)->format('g:i A') }}–{{ \Illuminate\Support\Carbon::parse($lane->ends_at)->format('g:i A') }}</h3><p class="mt-1 text-sm text-[#526474]">{{ $lane->plan->title }} · {{ number_format($lane->duration_minutes/60,1) }} hours weekly</p><dl class="mt-3 space-y-1 text-sm text-[#526474]"><div><dt class="inline font-semibold">Approximate location:</dt> <dd class="inline">{{ $laneArea ?: 'Area on file' }}</dd></div><div><dt class="inline font-semibold">Care activities:</dt> <dd class="inline">{{ $laneTasks ?: 'Review plan expectations after acceptance' }}</dd></div><div><dt class="inline font-semibold">Estimated caregiver earnings:</dt> <dd class="inline">{{ $laneEarningEstimates->get($lane->id) }}</dd></div><div><dt class="inline font-semibold">Respond by:</dt> <dd class="inline">{{ $lane->offer_expires_at?->setTimezone($lane->plan->timezone)->format('M j, g:i A T') ?: 'No deadline recorded' }}</dd></div></dl><div class="mt-4 grid grid-cols-2 gap-2"><button wire:click="acceptLane({{ $lane->id }})" wire:loading.attr="disabled" class="min-h-12 rounded-xl bg-amber-900 px-3 font-semibold text-white disabled:opacity-60">Accept recurring lane</button><button wire:click="declineLane({{ $lane->id }})" wire:loading.attr="disabled" class="min-h-12 rounded-xl border border-amber-300 px-3 font-semibold text-amber-900 disabled:opacity-60">Decline</button></div></article>@endforeach</div></div>@endif
            @if($offers->isNotEmpty())<div><h2 class="font-display text-2xl font-semibold">Optional backup shifts</h2><div class="mt-3 grid gap-4 lg:grid-cols-2">@foreach($offers as $offer)@php $offerArea=collect([data_get($offer->shift->plan->address_snapshot,'city'),data_get($offer->shift->plan->address_snapshot,'state'),data_get($offer->shift->plan->address_snapshot,'zip')])->filter()->implode(', '); $offerTasks=collect((array)$offer->shift->plan->task_snapshot)->pluck('name')->filter()->take(4)->implode(', '); @endphp<article class="rounded-3xl border border-sky-200 bg-sky-50 p-5"><p class="text-xs font-semibold uppercase tracking-wide text-sky-800">Family-approved backup offer</p><h3 class="mt-2 font-display text-xl font-semibold">{{ $offer->shift->scheduled_start_at->copy()->setTimezone($offer->shift->plan->timezone)->format('D, M j · g:i A') }}</h3><p class="mt-1 text-sm text-[#526474]">Until {{ $offer->shift->scheduled_end_at->copy()->setTimezone($offer->shift->plan->timezone)->format('g:i A T') }} · {{ number_format($offer->shift->scheduled_minutes/60,1) }} hours</p><dl class="mt-3 space-y-1 text-sm text-[#526474]"><div><dt class="inline font-semibold">Approximate location:</dt> <dd class="inline">{{ $offerArea ?: 'Area on file' }}</dd></div><div><dt class="inline font-semibold">Care activities:</dt> <dd class="inline">{{ $offerTasks ?: 'Coverage plan expectations' }}</dd></div><div><dt class="inline font-semibold">Estimated caregiver earnings:</dt> <dd class="inline">{{ $offerEarningEstimates->get($offer->id) }}</dd></div><div><dt class="inline font-semibold">Respond by:</dt> <dd class="inline">{{ $offer->expires_at?->setTimezone($offer->shift->plan->timezone)->format('M j, g:i A T') }}</dd></div></dl><div class="mt-4 grid grid-cols-2 gap-2"><button wire:click="acceptReplacement({{ $offer->id }})" wire:loading.attr="disabled" class="min-h-12 rounded-xl bg-sky-900 px-3 font-semibold text-white disabled:opacity-60">Accept shift</button><button wire:click="declineReplacement({{ $offer->id }})" wire:loading.attr="disabled" class="min-h-12 rounded-xl border border-sky-300 px-3 font-semibold text-sky-900 disabled:opacity-60">Decline</button></div></article>@endforeach</div></div>@endif
            @if($applications->isEmpty() && $opportunities->isEmpty() && $invitations->isEmpty() && $lanes->where('status','offered')->isEmpty() && $offers->isEmpty())<div class="rounded-3xl border border-dashed border-[#CFC4B5] bg-white p-8 text-center"><h2 class="font-display text-xl font-semibold">No coverage offers waiting</h2><p class="mt-2 text-[#607080]">New invitations appear here with the schedule, duration, location area, and earnings needed to decide.</p></div>@endif
        </section>
    @elseif($tab === 'commitments')
        <section class="space-y-5">
            <div class="rounded-2xl border border-[#D8D0C5] bg-[#FFF9F1] p-4 sm:flex sm:items-center sm:justify-between sm:gap-4">
                <div><p class="font-semibold text-[#17313F]">Keep your future availability accurate</p><p class="mt-1 text-sm text-[#526474]">Replacement matching also checks the availability saved on your caregiver profile.</p></div>
                <a href="{{ route('caregiver.onboarding') }}" wire:navigate class="mt-3 inline-flex min-h-11 items-center justify-center rounded-xl border border-[#2F6F62] px-4 font-semibold text-[#2F6F62] sm:mt-0">Manage availability</a>
            </div>
            <div class="grid gap-5 lg:grid-cols-2"><div class="rounded-3xl border border-[#DED6CA] bg-white p-5"><h2 class="font-display text-2xl font-semibold">Approved care teams</h2><div class="mt-4 space-y-3">@forelse($memberships as $member)<article class="rounded-2xl border border-[#E4DDD3] p-4"><div class="flex items-start justify-between gap-3"><div><p class="font-semibold">{{ $member->plan->title }}</p><p class="text-sm text-[#607080]">Family: {{ $member->plan->family?->name }}</p></div><span class="rounded-full px-3 py-1 text-xs font-semibold {{ $member->status==='active'?'bg-emerald-100 text-emerald-800':'bg-amber-100 text-amber-800' }}">{{ ucfirst($member->status) }}</span></div><p class="mt-3 text-sm text-[#526474]">{{ ucfirst($member->role) }} · {{ $member->replacement_opt_in?'Backup offers enabled':'Backup offers off' }}</p></article>@empty<p class="text-sm text-[#607080]">You have not joined a Continuous Coverage care team.</p>@endforelse</div></div><div class="rounded-3xl border border-[#DED6CA] bg-white p-5"><h2 class="font-display text-2xl font-semibold">Accepted recurring lanes</h2><div class="mt-4 space-y-3">@forelse($lanes->where('status','active') as $lane)<article class="rounded-2xl border border-[#E4DDD3] p-4"><p class="font-semibold">{{ ['Sunday','Monday','Tuesday','Wednesday','Thursday','Friday','Saturday'][$lane->day_of_week] }} · {{ \Illuminate\Support\Carbon::parse($lane->starts_at)->format('g:i A') }}–{{ \Illuminate\Support\Carbon::parse($lane->ends_at)->format('g:i A') }}</p><p class="mt-1 text-sm text-[#607080]">{{ $lane->plan->title }} · {{ number_format($lane->duration_minutes/60,1) }} hours</p></article>@empty<p class="text-sm text-[#607080]">No recurring coverage accepted yet.</p>@endforelse</div></div></div>
        </section>
    @else
        <section class="space-y-5">
            <div class="grid gap-4 sm:grid-cols-2"><div class="rounded-3xl bg-[#0F3D3E] p-5 text-white"><p class="text-sm text-white/75">Coverage earnings</p><p class="mt-2 font-display text-4xl font-semibold">${{ number_format($earningsCents/100,2) }}</p><p class="mt-2 text-sm text-white/75">Finalized caregiver earnings from linked coverage visits.</p></div><div class="rounded-3xl border border-[#DED6CA] bg-white p-5"><p class="text-sm text-[#607080]">Completed and past shifts</p><p class="mt-2 font-display text-4xl font-semibold">{{ $history->total() }}</p><a href="{{ route('caregiver.earnings.index') }}" wire:navigate class="mt-3 inline-block font-semibold text-[#2F6F62] underline">Open all earnings</a></div></div>
            <div class="rounded-3xl border border-[#DED6CA] bg-white p-5">
                <div class="flex flex-col gap-4 lg:flex-row lg:items-end lg:justify-between"><div><h2 class="font-display text-2xl font-semibold">Coverage history</h2><p class="mt-1 text-sm text-[#607080]">Past coverage remains available even after a visit, cancellation, or replacement.</p></div><div class="grid gap-3 sm:grid-cols-4"><label><span class="text-xs font-semibold text-[#526474]">Status</span><select wire:model.live="historyStatus" class="mt-1 min-h-11 w-full rounded-xl border-[#CFC4B5]"><option value="">All statuses</option>@foreach(['completed','cancelled','released','confirmed','in_progress','payment_attention'] as $status)<option value="{{ $status }}">{{ ucfirst(str_replace('_',' ',$status)) }}</option>@endforeach</select></label><label><span class="text-xs font-semibold text-[#526474]">From</span><input type="date" wire:model.change="historyFrom" class="mt-1 min-h-11 w-full rounded-xl border-[#CFC4B5]"></label><label><span class="text-xs font-semibold text-[#526474]">Through</span><input type="date" wire:model.change="historyThrough" class="mt-1 min-h-11 w-full rounded-xl border-[#CFC4B5]"></label><button wire:click="clearHistoryFilters" class="min-h-11 rounded-xl border border-[#CFC4B5] px-3 font-semibold text-[#526474]">Clear filters</button></div></div>
                <div class="mt-4 space-y-3">@forelse($history as $shift)@php $historyBooking=$historyBookings->get($shift->id); $wasReleased=(int)$shift->assigned_caregiver_user_id!==(int)auth()->id() && ($shift->released_by_user_id==auth()->id() || $shift->replacementCases->contains('original_caregiver_user_id',auth()->id())); $historyStatusLabel=$wasReleased?'Released / replaced':ucfirst(str_replace('_',' ',$shift->status)); $earnings=(int)($historyBooking?->payment?->caregiver_amount_cents??0); @endphp<article class="grid gap-3 rounded-2xl border border-[#E4DDD3] p-4 sm:grid-cols-[1fr_1fr_auto] sm:items-center"><div><p class="font-semibold">{{ $shift->scheduled_start_at->copy()->setTimezone($shift->plan->timezone)->format('D, M j, Y') }}</p><p class="text-sm text-[#607080]">{{ $shift->scheduled_start_at->copy()->setTimezone($shift->plan->timezone)->format('g:i A') }}–{{ $shift->scheduled_end_at->copy()->setTimezone($shift->plan->timezone)->format('g:i A') }}</p></div><div><p class="font-semibold">{{ $shift->plan->title }}</p><p class="text-sm text-[#607080]">{{ $historyStatusLabel }} · {{ number_format(($historyBooking?->worked_minutes?:$shift->scheduled_minutes)/60,1) }}h</p>@if($historyBooking)<a href="{{ route('caregiver.shifts.index') }}" wire:navigate class="mt-1 inline-block text-sm font-semibold text-[#2F6F62] underline">Open visit history</a>@endif</div><p class="text-lg font-semibold">${{ number_format($earnings/100,2) }}</p></article>@empty<p class="rounded-2xl bg-[#F7F2EA] p-6 text-center text-[#607080]">No coverage history matched these filters.</p>@endforelse</div>
                <div class="mt-5">{{ $history->links() }}</div>
            </div>
        </section>
    @endif
</div>
