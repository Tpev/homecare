@extends('layouts.marketing')
@section('title', ($hub->seo_title ?? null) ?: $hub->name.' Care Guides | LoLo Care')
@section('meta_description', ($hub->meta_description ?? null) ?: ($hub->description ?: 'Reviewed LoLo Care resources about '.$hub->name.'.'))
@section('canonical', $hubType === 'Category' ? route('blog.category',$hub) : route('blog.tag',$hub))
@section('feed_url', route('blog.feed'))
@section('content')
<div class="min-h-screen bg-[#F7F3ED]"><header class="border-b border-[#DED6CA] bg-white"><div class="mx-auto flex max-w-7xl items-center justify-between px-4 py-4 sm:px-6"><a href="{{ route('landing') }}" class="font-extrabold text-[#17313F]">LoLo Care</a><a href="{{ route('blog.index') }}" class="text-sm font-semibold text-[#0F5B52] underline">All resources</a></div></header><main class="mx-auto max-w-7xl px-4 py-12 sm:px-6 lg:px-8"><x-marketing.breadcrumbs :items="[['name'=>'Home','url'=>route('landing')],['name'=>'Resources','url'=>route('blog.index')],['name'=>$hub->name,'url'=>request()->url()]]"/><header class="mt-8 max-w-3xl"><p class="hc-brand-kicker">{{ $hubType }}</p><h1 class="mt-3 font-display text-5xl font-semibold">{{ $hub->name }}</h1><p class="mt-4 text-lg leading-8 text-[#526474]">{{ $hub->description }}</p></header><section class="mt-10 grid gap-6 md:grid-cols-2 lg:grid-cols-3">@forelse($presentedPosts as $item)<article class="overflow-hidden rounded-2xl border border-[#DED6CA] bg-white">@if($item['featured_media'])<a href="{{ $item['url'] }}"><img src="{{ $item['featured_media']->variantUrl('medium') }}" alt="{{ $item['featured_media']->alt_text }}" loading="lazy" class="aspect-[16/9] w-full object-cover"></a>@endif<div class="p-5"><h2 class="font-display text-2xl font-semibold"><a href="{{ $item['url'] }}">{{ $item['title'] }}</a></h2><p class="mt-2 text-sm leading-6 text-[#526474]">{{ $item['excerpt'] }}</p><p class="mt-4 text-xs text-[#6A7784]">{{ $item['author']?->name }} · {{ $item['published_at']?->format('M j, Y') }}</p></div></article>@empty<div class="rounded-2xl border border-dashed border-[#CFC6BA] bg-white p-8">No published resources in this topic yet.</div>@endforelse</section><div class="mt-10">{{ $posts->links() }}</div></main></div>
@endsection
