# Knowledge Entry: `{KB-ID}` — `{Title}`

Status: Draft

Type: `{product_fact/task_playbook/navigation/escalation}`

Version: 0.1

Owner: `{name/role}`

Optional reviewer/subject-matter contact: `{name/role or none}`

Self-review and publisher: `{authorized KB operator; may be the owner under DEC-022}`

Effective: `{date}`

Review by: `{date}`

Sensitivity: `{public/authenticated/shared_care/owner_only/restricted}`

## Applicability

- Roles: `{roles}`
- Membership/account states: `{states}`
- Product area: `{area}`
- Route/screen IDs: `{IDs}`
- Capability IDs: `{IDs}`
- Jurisdiction/locale: `{if applicable}`

## Approved sources

| Source ID | Section/anchor | Fact supported |
| --- | --- | --- |
| `{ID}` | `{section}` | `{fact}` |

## Approved answer or procedure

`{Plain-language content. Put exact wording in blockquotes when it must not be paraphrased.}`

## Facts the agent may state

- `{fact}`

## Facts the agent must not infer

- `{limitation}`

## Next approved actions

- `{navigation/tool/handoff}`

## Escalation conditions

- `{condition and playbook ID}`

## Retrieval examples

Should match:

- `{user wording}`

Should not match:

- `{neighboring wording and correct KB/capability}`

## Evaluation IDs

- `{EVAL-ID}`

## Change history

| Version | Date | Change | Sources revalidated | Evaluations run |
| --- | --- | --- | --- | --- |
| 0.1 | `{date}` | Initial draft | `{IDs}` | `{IDs}` |
